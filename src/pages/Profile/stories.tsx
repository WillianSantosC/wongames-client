import { Meta, StoryObj } from "@storybook/react";

import Profile from ".";

export default {
  title: "pages/Profile",
  component: Profile,
  parameters: {
    layout: "fullscreen",
  },
} as Meta;

export const Default: StoryObj = {};
